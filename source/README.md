# info

this source for use with flex. running without flex maybe made some unknown errors.
fuchsia is fully written in G. 

> NOTE:
> "siglib" must installed in your system. use ```sudo flex add siglib``` to install it.
